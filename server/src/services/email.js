const nodemailer = require('nodemailer');
async function sendOrderEmail({ buyer, order, sellerEmail }) { if (!process.env.SMTP_USER) return; const transport = nodemailer.createTransport({ host: process.env.SMTP_HOST, port: Number(process.env.SMTP_PORT || 587), secure: false, auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS } }); const from = process.env.SMTP_FROM || process.env.SMTP_USER; await Promise.all([transport.sendMail({ from, to: buyer.email, subject: `Order #${order._id} confirmed`, text: `Thanks ${buyer.name}! Your order total is ₹${order.total}. ETA: ${order.delivery.eta}` }), sellerEmail && transport.sendMail({ from, to: sellerEmail, subject: 'New Hype order', text: `A new order has been placed for ₹${order.total}.` })]); }
module.exports = { sendOrderEmail };
